app.message=This is QC environment settings ${spring.application.name}

# Context path
server.servlet.context-path=/qc

# MongoDB database
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=superblog